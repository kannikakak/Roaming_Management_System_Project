-- Add zeroRevenueRate tracking to data quality history if you store snapshots.
-- This is a placeholder showing the migration file naming convention:
-- NNNN_description.sql  (zero-padded number, underscore-separated description)
-- Each file runs exactly once and is recorded in the schema_migrations table.
SELECT 1;
